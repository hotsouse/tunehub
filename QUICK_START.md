# 🚀 Быстрый запуск проекта

## Вариант 1: Локальный запуск (рекомендуется для разработки)

### Шаг 1: Активация виртуального окружения

**В PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
```

**Если возникла ошибка выполнения скриптов**, выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**В Command Prompt (CMD):**
```cmd
venv\Scripts\activate.bat
```

### Шаг 2: Установка/обновление зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 3: Применение миграций БД (включая новые индексы)

```bash
python manage.py migrate
```

### Шаг 4: Создание суперпользователя (опционально, для доступа к админке)

```bash
python manage.py createsuperuser
```

Введите:
- Username (имя пользователя)
- Email (опционально)
- Password (пароль - минимум 8 символов, заглавные, строчные, цифры)

### Шаг 5: Загрузка тестовых данных (опционально)

```bash
python manage.py load_sample_music
python manage.py load_sample_movies
```

### Шаг 6: Запуск сервера разработки

```bash
python manage.py runserver
```

### Шаг 7: Откройте в браузере

- **Главная страница**: http://127.0.0.1:8000/
- **Админка**: http://127.0.0.1:8000/admin/
- **Swagger API**: http://127.0.0.1:8000/api/docs/
- **Health Check**: http://127.0.0.1:8000/api/health/

---

## Вариант 2: Запуск через Docker (для полного окружения с мониторингом)

### Требования
- Docker Desktop должен быть установлен и запущен

### Шаг 1: Запуск всех сервисов

```bash
docker-compose up --build
```

Это запустит:
- ✅ Django приложение (порт 8000)
- ✅ PostgreSQL база данных
- ✅ Redis
- ✅ Prometheus (порт 9090)
- ✅ Grafana (порт 3000)

### Шаг 2: Создание суперпользователя (в новом терминале)

```bash
docker-compose exec web python manage.py createsuperuser
```

### Шаг 3: Доступ к сервисам

- **Приложение**: http://localhost:8000
- **Prometheus**: http://localhost:9090
- **Grafana**: http://localhost:3000 (логин: admin/admin)

### Остановка Docker

```bash
docker-compose down
```

---

## 🔍 Проверка работы

### Проверка здоровья API

```bash
curl http://127.0.0.1:8000/api/health/
```

Или откройте в браузере: http://127.0.0.1:8000/api/health/

### Проверка API документации

Откройте Swagger UI: http://127.0.0.1:8000/api/docs/

### Тестирование API регистрации

```bash
curl -X POST http://127.0.0.1:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@example.com","password1":"TestPass123","password2":"TestPass123"}'
```

---

## 🐛 Решение проблем

### Проблема: Порт 8000 занят

**Решение:**
```bash
python manage.py runserver 8001
```
Затем откройте: http://127.0.0.1:8001/

### Проблема: Ошибка "ModuleNotFoundError"

**Решение:**
1. Убедитесь, что виртуальное окружение активировано
2. Переустановите зависимости:
```bash
pip install -r requirements.txt
```

### Проблема: Ошибка миграций

**Решение:**
```bash
python manage.py makemigrations
python manage.py migrate
```

### Проблема: Статические файлы не найдены

**Решение:**
```bash
python manage.py collectstatic --noinput
```

### Проблема: База данных заблокирована (SQLite)

**Решение:**
1. Закройте все подключения к БД
2. Удалите `db.sqlite3` (если это тестовая БД)
3. Выполните миграции заново:
```bash
python manage.py migrate
```

---

## 📝 Полезные команды

### Запуск тестов
```bash
python manage.py test
```

### Запуск тестов с покрытием
```bash
coverage run manage.py test
coverage report
coverage html  # создаст HTML отчет в htmlcov/
```

### Проверка безопасности
```bash
bandit -r .
```

### Создание суперпользователя
```bash
python manage.py createsuperuser
```

### Просмотр всех URL
```bash
python manage.py show_urls
```

---

## ✅ Что должно работать после запуска

1. ✅ Главная страница открывается
2. ✅ Можно зарегистрироваться и войти
3. ✅ API доступен через `/api/`
4. ✅ Swagger документация работает
5. ✅ Health checks отвечают
6. ✅ Админка доступна (если создан superuser)

---

## 🎯 Следующие шаги

1. **Изучите API**: http://127.0.0.1:8000/api/docs/
2. **Прочитайте документацию**: `USER_MANUAL.md`, `SETUP_GUIDE.md`
3. **Настройте production**: см. `SETUP_GUIDE.md`
4. **Запустите тесты**: `python manage.py test`

---

**Готово! Проект запущен! 🎉**

