# 🔐 Админ-панель и вход в систему

## Проблема с входом

Если вы видите сообщение "Please enter a correct username and password", это означает, что в базе данных еще нет пользователя с правами администратора.

**Решение:** Создайте суперпользователя (администратора) одним из способов ниже.

## Способ 1: Быстрая команда (рекомендуется)

Используйте специальную команду для создания администратора:

```bash
python manage.py create_admin
```

Команда запросит:
- Имя пользователя (по умолчанию: `admin`)
- Email (по умолчанию: `admin@example.com`)
- Пароль (будет запрошен дважды для подтверждения)

### Примеры использования:

```bash
# Создать администратора с настройками по умолчанию
python manage.py create_admin

# Создать администратора с указанными данными
python manage.py create_admin --username admin --email admin@example.com --password ваш_пароль

# Создать администратора без интерактивного ввода (для скриптов)
python manage.py create_admin --username admin --password ваш_пароль --no-input
```

## Способ 2: Стандартная команда Django

```bash
python manage.py createsuperuser
```

Эта команда запросит:
- Username (имя пользователя)
- Email address (email адрес)
- Password (пароль - будет запрошен дважды)

## Доступ к админ-панели

После создания суперпользователя:

1. **Запустите сервер** (если еще не запущен):
   ```bash
   python manage.py runserver
   ```

2. **Откройте админ-панель Django** в браузере:
   ```
   http://127.0.0.1:8000/admin/
   ```

3. **Войдите** используя созданные учетные данные (username и password)

## Что можно делать в админ-панели Django

Админ-панель Django (`/admin/`) позволяет управлять:

### 👥 Пользователи (Accounts → Custom Users)
- Просматривать всех пользователей
- Создавать новых пользователей
- Редактировать существующих пользователей
- Устанавливать права доступа (`is_staff`, `is_superuser`)
- Управлять избранными фильмами и музыкой пользователей

### 🎬 Фильмы (Movies)
- **Movies** - добавлять, редактировать, удалять фильмы
- **Genres** - управлять жанрами фильмов
- **Directors** - управлять режиссерами
- **Actors** - управлять актерами

### 🎵 Музыка (Music)
- **Tracks** - добавлять, редактировать, удалять треки
- **Albums** - управлять альбомами
- **Artists** - управлять исполнителями
- **Genres** - управлять музыкальными жанрами
- **Playlists** - просматривать плейлисты пользователей

## Важные замечания

1. **Пароль должен быть минимум 8 символов** и соответствовать требованиям валидации:
   - Содержать заглавные буквы
   - Содержать строчные буквы
   - Содержать цифры
   - Не быть полностью числовым

2. **Права доступа:**
   - `is_staff = True` - позволяет войти в админ-панель
   - `is_superuser = True` - дает полные права администратора

3. **Если забыли пароль администратора:**
   ```bash
   python manage.py create_admin --username ваш_username --password новый_пароль
   ```
   Это обновит пароль существующего пользователя.

## Альтернативный способ входа

Если вы хотите войти через обычную страницу входа (`/accounts/login/`), используйте те же учетные данные, что и для админ-панели.

## Проверка существующих пользователей

Чтобы проверить, какие пользователи уже созданы:

```bash
python manage.py shell
```

Затем в Python shell:
```python
from accounts.models import CustomUser
users = CustomUser.objects.all()
for user in users:
    print(f"Username: {user.username}, Email: {user.email}, Staff: {user.is_staff}, Superuser: {user.is_superuser}")
```

## Решение проблем

### Проблема: "Пользователь уже существует"
- Используйте другое имя пользователя или обновите пароль существующего пользователя

### Проблема: "Не могу войти в админ-панель"
- Убедитесь, что у пользователя установлено `is_staff = True`
- Проверьте правильность имени пользователя и пароля
- Убедитесь, что сервер запущен

### Проблема: "Пароль не соответствует требованиям"
- Используйте пароль минимум 8 символов
- Включите заглавные и строчные буквы, цифры

